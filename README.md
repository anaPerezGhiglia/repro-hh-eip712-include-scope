# Repro: Hardhat 3 EIP-712 collector throws `HHE818` for a same-named struct in a non-included source

Minimal dual-toolchain reproduction for an EIP-712 type-collector scoping bug in `hardhat@3.6.0`. The same project, with equivalent config, runs cleanly under Forge — proving the divergence is a Hardhat-side scoping decision rather than a project-level ambiguity.

## TL;DR

`test.solidity.eip712Types.include` (Hardhat) and `[bind_json].include` (Forge) are both declarations of which Solidity files supply EIP-712 type definitions. The expectation — and Forge's actual behavior — is that `include` bounds the set of structs that can register names and trigger name-collision errors.

Hardhat does not honor this scope. `collectEip712CanonicalTypes` (`solidity-test/eip712/index.ts:91-116` in `3.6.0`) walks every source's AST and pushes its structs into the registry unconditionally; `include`/`exclude` is only consulted to populate `selectedNames` at emit time. A same-named struct in an *unincluded, unimported* file collides at `canonicalize.ts:indexByName` with the selected struct and throws `HHE818` — even though the user explicitly scoped that other file out.

Forge does not have this divergence — `[bind_json].include` controls the `target_files` set, and only structs in `target_files` enter the registry. See [`crates/forge/src/cmd/bind_json.rs`](https://github.com/foundry-rs/foundry/blob/master/crates/forge/src/cmd/bind_json.rs) (≈ lines 159-213, 280-286).

## Project shape

```
.
├── contracts/
│   ├── A.sol      ← in eip712Types.include
│   │              defines `library A { struct Order { address, uint256 } }`
│   └── B.sol      ← NOT included, NOT imported by A.sol or the test
│                    defines a same-named `struct Order { bytes32, bool }`
├── test/
│   └── Foo.t.sol  ← uses vm.eip712HashStruct(...) against A.Order
├── hardhat.config.ts     ← eip712Types: { include: ["contracts/A.sol"] }
├── foundry.toml          ← src/test/solc/evm_version only
└── remappings.txt        ← forge-std/=node_modules/forge-std/src/
```

`contracts/A.sol` has zero imports — `contracts/B.sol` is not reachable from the selected file by any Solidity import path.

## Reproducing

### Recommended: open in the bundled devcontainer

The repo ships a `.devcontainer/devcontainer.json` that pins a Debian 12 (bookworm) base image so the GLIBC is new enough to run the latest stable Foundry. `postCreateCommand` installs Foundry via `foundryup` and runs `npm install` for you.

In VS Code: "Reopen in Container" → wait for `postCreateCommand` to finish → then:

```bash
npm test               # Hardhat — fails with HHE818
forge test             # Foundry — passes
```

### Or run locally

If you'd rather skip the devcontainer, the project needs:

- Node `≥ 22`
- Foundry stable (`foundryup` against current libc — needs GLIBC `≥ 2.32`, so Ubuntu 22.04 / Debian 12 or newer)
- `npm install` to pull `hardhat@^3.6.0` + `forge-std`

Then:

```bash
npm test               # Hardhat — fails with HHE818
forge test             # Foundry — passes
```

### Hardhat output

```
Error HHE818: Two different EIP-712 struct definitions named "Order" were found:
- contracts/A.sol
- contracts/B.sol
```

Compilation succeeds; the throw originates in the test runner's EIP-712 type collector (`solidity-test/eip712/canonicalize.ts:242`) before any test runs.

### Forge output

```
Ran 1 test for test/Foo.t.sol:FooTest
[PASS] test_orderHash() (gas: 4996)
Suite result: ok. 1 passed; 0 failed; 0 skipped
```

Same project, same struct definitions, same toolchain inputs, opposite outcomes.

## Self-test: confirm it's the scope issue

Delete `contracts/B.sol` and re-run `npm test` — Hardhat passes too. Add `B.sol` back, even though no source file imports it and no test references it, and Hardhat breaks again. Forge is unaffected either way.

## A note on the cheatcode form used in the test

`vm.eip712HashStruct(typeNameOrDefinition, abiEncodedData)` accepts either a bare type **name** (resolved at runtime through the file generated by `forge bind-json`) or a literal **type definition** string. The test in this repro uses the **definition** form:

```solidity
vm.eip712HashStruct("Order(address user,uint256 amount)", abi.encode(o));
```

That choice intentionally keeps the repro independent of `forge bind-json`. The bug being demonstrated is unaffected: Hardhat's `HHE818` is thrown at collection time, before any cheatcode call runs — so whether the test uses the name form or the definition form doesn't change the outcome on either side.

In a real project, the name form is more common (it's what the bound `JsonBindings.sol` is for). To validate the Forge-side claim under the name form too:

1. Add `fs_permissions = [{ access = "read", path = "test/JsonBindings.sol" }]` and `[bind_json] out = "test/JsonBindings.sol"`, `include = ["contracts/A.sol"]` to `foundry.toml`.
2. Run `forge bind-json` against this project. The output `JsonBindings.sol` will contain exactly one `schema_Order = "Order(address user,uint256 amount)"` line — A.sol's struct, with no entry for B.sol. The same structure is visible in [aave-v4's checked-in JsonBindings.sol](https://github.com/aave/aave-v4/blob/hh3-migration-v2/tests/helpers/mocks/JsonBindings.sol), generated by the same flow on a real project.
3. Switch the cheatcode call in `Foo.t.sol` to the name form: `vm.eip712HashStruct("Order", abi.encode(o))`.
4. `forge test` still passes; `npm test` still throws `HHE818`.

The devcontainer's stable Foundry handles this correctly. (Some older or custom-built Foundry binaries floating around — e.g. `forge 1.7.1-dev` built against GLIBC 2.31 hosts — have a regression where `forge bind-json` silently emits an empty `library JsonBindings { ... }` body. If you hit that, the devcontainer in this repo is the cleanest workaround.)

## Versions

- `hardhat@3.6.0` (also reproduces on `3.5.1`)
- `@nomicfoundation/edr@0.12.0-next.33`
- `forge` — whichever current stable `foundryup` installs (verified against `1.7.x` series). The devcontainer installs this for you.
- `forge-std` pinned to `60acb7a` (the commit that introduced `vm.eip712HashStruct`)
- Node `22` (provisioned by the devcontainer's Node feature)
- Base image: `mcr.microsoft.com/devcontainers/base:bookworm` (Debian 12, GLIBC `2.36`)

## Why it matters

A project that has scoped EIP-712 types correctly under Forge cannot adopt Hardhat without first auditing every Solidity file in the repo for accidental name collisions against the selected struct set — even across feature areas that have nothing to do with EIP-712 cheatcodes. The bug expands the surface that the user has to keep clean from "the include set" to "the entire codebase."

## Related

Distinct from a companion bug where the same test runner crashes with `ERR_STRING_TOO_LONG` on very large compilation sets; that bug masks this one on big projects (e.g. `aave/aave-v4@hh3-migration-v2`). This repro is intentionally tiny so the `HHE818` throw fires directly, with no patches required.
